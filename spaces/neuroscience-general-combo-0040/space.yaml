schema_version: '2.0'
title: 'Neuroscience: General (COMBO_0040)'
description: Neuroscience general comparative space with 3 models, configured as non-causal
  co-simulation.
models:
- repo: Biosimulant/models-neuroscience-neuroml-01
  alias: cat_t_1
  manifest_path: models/neuroscience-neuroml-cat-t-type-transient-low-threshold-inactivating-nmlch001571-model/model.yaml
  parameters:
    model_path: data/render_xml_file.nml
- repo: Biosimulant/models-neuroscience-neuroml-01
  alias: cat_t_2
  manifest_path: models/neuroscience-neuroml-cat-t-type-transient-low-threshold-inactivating-nmlch001598-model/model.yaml
  parameters:
    model_path: data/render_xml_file.nml
- repo: Biosimulant/models-neuroscience-neuroml-01
  alias: catmaidshowcase_3
  manifest_path: models/neuroscience-neuroml-catmaidshowcase-catmaidshowcase-model/model.yaml
  parameters:
    model_path: data/repo
- repo: Biosimulant/models-observability
  alias: state_compare_mon
  manifest_path: models/observability-state-comparison-monitor/model.yaml
  parameters:
    max_points: 2000
    min_dt: 1.0
- repo: Biosimulant/models-observability
  alias: state_metrics_mon
  manifest_path: models/observability-state-metrics-monitor/model.yaml
  parameters:
    max_points: 2000
    min_dt: 1.0
runtime:
  duration: 0.3
  tick_dt: 0.001
  initial_inputs: {}
wiring:
- from: cat_t_1.state
  to:
  - state_compare_mon.state_a
  - state_metrics_mon.state_a
- from: cat_t_2.state
  to:
  - state_compare_mon.state_b
  - state_metrics_mon.state_b
- from: catmaidshowcase_3.state
  to:
  - state_compare_mon.state_c
  - state_metrics_mon.state_c
scientific_context:
  question: How do general mechanisms compare across these models?
  mode: comparative
  assumptions:
  - Model implementations are used as published in their curated manifests without
    biological reinterpretation.
  - Time integration uses a shared global tick compatible with model min_dt constraints.
  - Comparative (non-causal) mode is used because full deterministic IO coverage for
    causal coupling was not satisfied.
  expected_observables:
  - Model output trajectories under shared runtime settings.
  - Cross-model agreement/divergence in key state or metric signals.
  - Relative behavior comparison without causal linkage claims.
  limitations:
  - No new biology is introduced beyond what upstream models encode.
  - Cross-model semantic matching is rule-based and may under-connect uncertain routes.
  provenance:
  - 'neuroscience-neuroml-cat-t-type-transient-low-threshold-inactivating-nmlch001571-model
    :: neuroml_db:NMLCH001571 :: https://neuroml-db.org/model_info?model_id=NMLCH001571'
  - 'neuroscience-neuroml-cat-t-type-transient-low-threshold-inactivating-nmlch001598-model
    :: neuroml_db:NMLCH001598 :: https://neuroml-db.org/model_info?model_id=NMLCH001598'
  - 'neuroscience-neuroml-catmaidshowcase-catmaidshowcase-model :: opensourcebrain:CATMAIDShowcase
    :: https://github.com/OpenSourceBrain/CATMAIDShowcase'
  visualization_rationale: A dedicated monitor model receives all participating model
    state streams (`state_a..state_d`) so trajectories can be compared in one place
    without claiming causal coupling when IO semantics are incomplete.
